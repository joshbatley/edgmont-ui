module.exports = {
  content: [],
  theme: {
    colors: ({ colors }) => ({
      gray: colors.slate,
      green: colors.emerald,
      red: colors.red,
      yellow: colors.amber,
      blue: colors.blue,
      purple: colors.indigo,
      teal: colors.teal,
      pink: colors.pink,
      white: colors.white,
      transparent: colors.transparent,
      current: colors.currentColor,
    }),
  },
}
